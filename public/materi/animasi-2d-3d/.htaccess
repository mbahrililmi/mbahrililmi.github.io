Options -MultiViews
RewriteEngine On

# Remove .html extension from URLs
# e.g. /animasi-2d-3d-p2 → /animasi-2d-3d-p2.html
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L,QSA]
